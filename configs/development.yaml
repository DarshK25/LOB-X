environment: development

database:
  host: localhost
  port: 5432
  name: lobx_dev
  user: lobx_user
  pool_size: 5

redis:
  host: localhost
  port: 6379

api:
  host: 0.0.0.0
  port: 8000
  reload: true
  workers: 1

simulation:
  tick_ms: 100
  seed: 42
  mid_price: 10000

logging:
  level: DEBUG
  format: "%(asctime)s %(name)s %(levelname)s %(message)s"
